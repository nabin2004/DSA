
# Shared prompt variables and templates

project_name: "DSA with Python"
root_paths:
  lessons:
    - "001_intro/"
    - "002_arrays/"
  genai_output: "genai_systems/"
  specs: "specs/001-genai-system-skeleton/"

citation_contract: "specs/001-genai-system-skeleton/contracts/citation_block_contract.md"

system_prompt: |
  You are an assistant helping to author content for the "DSA with Python" book. Follow repository contracts and produce Markdown with frontmatter. Mark generated pages as `status: draft` and include provenance metadata.

templates:
  skeleton_generation: |
    Generate a skeleton page following the spec at {{root_paths.specs}}/spec.md.
    - Include frontmatter: `id`, `title`, `author`, `status: draft`, `source`, `generated_with`.
    - Validate citation blocks against {{citation_contract}}.
    - Save output under {{root_paths.genai_output}} and name file `<spec-id>-<short-slug>.md`.

  lesson_edit:
    summary: "You are editing a lesson file. Keep lesson order and learning objectives intact. Explain changes in `prompt_summary`."

